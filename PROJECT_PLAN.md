# Briefing Atlas · 科技简报图志制作方案

文档日期：2026-09-08  
文档状态：初步制作方案，尚未开始网站开发  
网站副标题：AI & Tech Briefing

## 1. 项目定位

制作一个以日历为入口的个人科技简报档案库，将持续增长的聊天简报整理为独立、可检索、可长期维护的网站内容。

重点解决三个问题：

1. 按年、月、日快速定位历史简报，无须翻阅长对话。
2. 根据关键词、公司、技术或主题找到过去的具体新闻。
3. 建立独立于聊天记录的内容存储与更新流程，便于备份和迁移。

已有简报以每天约 5 条精选为主，关注 AI、机器学习、大模型、游戏开发 / Vibe Gaming 和科技行业，侧重行业影响力，同时涵盖趋势观点和新论文。网站保留“发生了什么、为什么重要、实践或研究启示”的阅读结构。

首版的核心验收目标：用户打开网站后，可以通过日期找到某一期简报，也可以通过一个关键词找到某条历史新闻并查看来源。

## 2. 项目名称

- 英文名称：Briefing Atlas
- 中文名称：科技简报图志
- 网站副标题：AI & Tech Briefing
- 建议仓库名称：briefing-atlas

名称表达“按时间与主题浏览知识档案”的用途。仓库名与现有项目目录名可以不同，不需要为了名称调整本地目录。

## 3. 隐私与部署边界

“私人网站”需要区分两种需求：

| 使用方式 | 方案影响 |
| --- | --- |
| 个人使用，但新闻与简报允许公开访问 | 适合使用 GitHub Pages 托管静态网站 |
| 仅自己登录后可查看，内容不能公开 | 需要进一步设计访问控制或调整托管方案 |

GitHub Pages 的站点访问权限与仓库是否私有并不等价。根据 GitHub 官方文档，私有发布与访问控制适用于使用 GitHub Enterprise Cloud 的组织所拥有的相应项目站点。普通个人 GitHub Pages 不能直接作为仅本人可访问的私密资料库。

本方案暂以“个人使用、发布内容可以公开”为工作假设，不代表已经确认公开发布。正式部署前应明确内容可见性。

如果需要严格私密，优先考虑支持身份验证的托管与数据方案。仅增加前端密码框、隐藏网址或禁止搜索引擎收录，不能保护已经公开发布的正文、JSON 文件或搜索索引。

参考：[GitHub Pages 站点可见性说明](https://docs.github.com/en/enterprise-cloud@latest/pages/getting-started-with-github-pages/changing-the-visibility-of-your-github-pages-site)

## 4. 功能范围与优先级

### 4.1 第一期：归档与检索

| 功能 | 具体行为 |
| --- | --- |
| 最新简报首页 | 默认展示最新已发布的一期，显示简报日期、摘要和新闻目录；当天无更新时明确标注 |
| 日历归档 | 切换年份和月份；有内容的日期显示标记；点击日期进入简报 |
| 日期直达 | 支持直接选择或输入日期，避免逐月翻找 |
| 归档列表 | 提供按月分组的简报列表，作为日历之外的浏览入口 |
| 每日简报详情 | 展示完整正文、目录、标签、来源与前后期导航 |
| 单条新闻定位 | 每条新闻有稳定锚点，可复制直达链接 |
| 全文搜索 | 检索标题、摘要与正文，支持中文、英文和混合术语 |
| 组合筛选 | 按日期范围、主题、公司 / 机构缩小搜索结果 |
| 移动端适配 | 日历可折叠，正文适应窄屏，避免横向溢出 |
| 基础阅读设置 | 深浅色模式、字号调整、摘要与全文切换 |

### 4.2 第二期：个人阅读管理

- 收藏单条新闻，并提供独立收藏页面。
- 区分未读、已读，支持手动修改状态。
- 记录上次阅读的简报与位置，提供继续阅读入口。
- 按共同标签、公司或同一事件展示相关报道。
- 导出、导入收藏和阅读记录，支持备份与更换设备时手动迁移。

上述个人状态首版保存在当前浏览器，不自动跨设备同步。清理浏览器站点数据可能删除记录，因此备份功能与收藏功能一并设计。个人笔记和阅读记录不写入公开网站内容。

### 4.3 第三期：持续更新与主题积累

- 建立每日简报的标准化导入流程。
- 对日期、必要字段、重复编号和来源链接进行检查。
- 识别可能重复的新闻，区分重复报道与事件新进展。
- 生成周报、月报和主题时间线，并保留原始新闻链接。
- 根据实际需要增加 RSS、离线阅读或跨设备同步。

首版不加入聊天式问答、账号体系或在线内容编辑后台，先验证内容归档和阅读体验。需要严格私密时，身份验证应提前纳入基础架构，不推迟到第三期。

## 5. 页面与导航

### 5.1 首页

顶部包含网站名称、搜索入口、归档入口及主题切换。主要内容展示最新一期的日期、当日主线、新闻目录和摘要卡片。补充最近几期入口，减少回到日历查找的操作。

“最新一期”与“今日简报”分开处理：仅当最新内容的简报日期等于当前约定时区日期时，使用“今日”表述。

### 5.2 归档页

桌面端使用左侧日历与筛选、右侧简报列表的布局。移动端将日期选择器放在列表上方，并可折叠。

日历行为：

- 默认定位最新一期所在月份。
- 区分今天、当前选中日期、有简报日期和无简报日期。
- 支持跳转到指定年月及最新一期。
- 无内容日期显示“暂无简报”，不自动跳转到其他日期。
- 选择年份时显示各月简报数量，方便跨年查找。
- 颜色之外提供文字或图形标记，支持键盘操作。

### 5.3 每日简报页

页面顺序为：日期与标题 → 当日摘要 → 新闻目录 → 各条新闻正文 → 来源与更新信息 → 前后期导航。

每条新闻保留以下阅读层次：

1. 标题与标签。
2. 发生了什么。
3. 为什么重要。
4. 实践或研究启示。
5. 原始报道、官方公告或论文链接。

“上一期 / 下一期”跳转到实际存在的相邻简报，不假设每日都有内容。来源缺失、图片缺失或旧内容尚未核验时，应显示对应状态。

### 5.4 搜索与主题页

搜索以单条新闻为结果单位，显示标题、所属简报日期、命中摘要和标签。点击结果直达对应新闻。

支持关键词、日期范围和主题共同生效，并允许一键清空筛选。无结果时说明当前条件，提供放宽范围的入口。主题页复用搜索与筛选能力，减少重复开发。

## 6. 视觉与交互方向

采用简洁的个人阅读工具风格，以阅读效率为主：

- 正文使用舒适行距和受控阅读宽度，避免大屏上一行文字过长。
- 使用克制的强调色突出日期、链接和选中状态。
- 图片作为内容补充，不要求每条新闻必须有大图。
- 支持中文排版、英文术语、代码片段和数学公式。
- 长页面提供目录和返回顶部；滚动时保持必要导航可用。
- 图片预留尺寸，加载失败提供占位说明，避免页面跳动。
- 搜索框、日期选择和弹出面板具备明确焦点状态。
- 动效保持轻量，并尊重减少动态效果的系统偏好。

## 7. 技术架构建议

建议采用“静态页面 + 独立内容文件 + 浏览器端交互”的架构，适配 GitHub Pages。

| 层次 | 建议 |
| --- | --- |
| 页面生成 | 优先评估 Astro，生成每日页面和归档页面 |
| 交互逻辑 | TypeScript，实现日历、筛选、搜索及阅读状态 |
| 正文内容 | Markdown，便于人工修改和长期保存 |
| 元数据 | 使用结构化字段，保存日期、编号、标签和来源 |
| 搜索 | 构建时生成索引，浏览器按需加载；使用真实中英文样例确定检索实现 |
| 个人状态 | 浏览器本地存储；记录增多后再评估 IndexedDB |
| 构建发布 | 后续通过 GitHub Actions 构建并发布到 GitHub Pages |

以上是选型方向，具体依赖与版本在实施时核对官方文档后确定。纯静态站点可以支持日历、搜索和本地收藏；自动采集新闻、调用模型、账户登录和跨设备写入需要另外的执行环境或服务。

路由采用可生成静态文件的结构，例如 `/briefings/2026-09-08/`。所有资源链接必须适配项目子路径部署，不能假设网站位于域名根目录。直接打开、刷新和复制深层链接均应可用。

## 8. 内容模型与目录

使用“每日一期、每期多条新闻”的结构。一天多次修订应更新同一期并保存修订时间，避免无意生成重复日期页面。

### 8.1 简报字段

| 字段 | 含义 |
| --- | --- |
| `id` | 稳定的简报编号 |
| `briefingDate` | 简报归档日期，格式为 YYYY-MM-DD |
| `title` | 简报标题 |
| `summary` | 当日主线或导读 |
| `publishedAt` | 发布时间，包含时区偏移 |
| `updatedAt` | 最近修订时间 |
| `status` | 草稿或已发布 |
| `stories` | 当期新闻条目 |

### 8.2 新闻字段

| 字段 | 含义 |
| --- | --- |
| `id` | 稳定编号，不随排序改变 |
| `title` | 新闻标题 |
| `summary` | 简短摘要 |
| `body` | 正文，包含事实、影响和启示 |
| `eventDate` | 事件日期；不确定时允许为空 |
| `tags` | 主题标签 |
| `entities` | 公司、机构、产品或人物 |
| `sources` | 来源标题、具体 URL、来源类型及可用的发布时间 |
| `image` | 可选图片、说明、来源与替代文本 |
| `verificationStatus` | 待核验、已核验或需更正，并配套核验说明 |
| `relatedEventId` | 可选，用于关联同一事件的后续报道 |

日历按 `briefingDate` 归档，不使用事件日期替代。归档默认以 Asia/Shanghai 为日期边界；不能因访问者浏览器时区不同而改变既有归档日期。来源发布时间、事件日期和简报日期分别保存。

建议目录如下，仅表示相对项目结构：

```text
content/
  briefings/
    2026/
      09/
        2026-09-08.md
src/
  components/
  layouts/
  pages/
  styles/
  lib/
public/
  images/
scripts/
  import/
  validate/
```

Markdown 中包含元数据与分条正文，具体序列化方式在导入样例阶段确定。搜索索引和页面从内容源生成，避免人工维护多份相同正文。

## 9. 历史简报迁移

先迁移少量具有代表性的历史简报，覆盖跨月日期、长正文、论文、公式、多来源和图片缺失等情况，再批量迁移。

迁移步骤：

1. 提取每期日期、标题、导读及新闻条目。
2. 将正文转换为标准 Markdown，保留原有层次。
3. 将可以解析的引用转换为具体文章链接。
4. 清理聊天专用的图片、实体和引用组件标记。
5. 规范标签、公司别名和新闻编号。
6. 检查重复日期、缺失来源、异常时间及内容完整性。
7. 生成人工可检查的迁移摘要，再导入网站。

现有简报中的聊天引用编号不是可公开使用的来源 URL；图片查询组件也不等于可直接展示的图片文件。无法恢复的来源或图片标注待补全，不编造链接，也不使用公司首页冒充具体报道来源。

历史内容导入与事实核验分开记录。导入成功不代表新闻已经证实，未经核验的历史条目不得自动标记为已核验。后续更正应保留更新时间和更正说明。

## 10. 持续更新流程

目标流程：

```text
产生简报内容
  → 整理为统一格式
  → 校验日期、字段、重复条目和来源
  → 预览检查
  → 更新内容仓库
  → 自动构建
  → 发布网站
```

分两步推进：

- 首先提供手动导入流程，通过标准 Markdown 或结构化文件更新内容。
- 手动流程稳定后，再验证现有简报任务的可用输出方式，选择定时脚本、工作流或其他集成方式自动归档。

目前不假设已有任务能自动写入 GitHub，也不把聊天记录读取能力等同于稳定的自动同步接口。自动化实施前需要确认内容获取方式、运行位置、凭据管理、执行成本和失败恢复。

自动流程应支持重复执行而不重复创建同一期；构建或校验失败时保留上一版可用网站。新闻检索和模型调用如需密钥，应在执行环境中管理，不能放入前端代码、公开内容或搜索索引。

## 11. 实施阶段与交付物

| 阶段 | 工作内容 | 交付物与完成条件 |
| --- | --- | --- |
| A：需求与样例 | 明确公开或私密，选取真实样例，确定字段和标签 | 可见性方案、内容规范、可用样例 |
| B：可浏览原型 | 首页、日历、每日页面、移动端布局 | 可以从日期进入简报，直达链接可刷新 |
| C：检索与历史迁移 | 搜索、组合筛选、导入和内容校验 | 能检索样例与历史内容，来源状态清楚 |
| D：首版发布 | 构建配置、子路径适配、正式内容检查 | 在已确定的可见性方案下正常访问 |
| E：阅读管理 | 收藏、已读、阅读位置、备份 | 个人状态可持久保存、导出和恢复 |
| F：自动更新 | 验证接入、重复检查、失败恢复 | 新简报可可靠入库，失败不覆盖可用版本 |

先完成 A 至 D，验证网站解决实际查找问题，再推进后续功能。当前阶段仅编写方案，不执行 Git 初始化、提交、远程仓库创建或部署。

## 12. 验收与验证

### 日期与导航

- 年月切换、跨年、闰日、空月份和无简报日期行为正确。
- 前后期导航能跨越缺更日期。
- 简报日期在不同时区的浏览器中保持一致。
- 日历与归档列表指向同一份内容。

### 搜索与内容

- 中文、英文、大小写与混合术语的真实查询可以找到预期新闻。
- 日期范围与标签组合筛选正确，无结果提示明确。
- 搜索结果跳转到正确新闻，修改排序后旧锚点仍有效。
- Markdown、代码、公式与图片缺失情况均可正常阅读。
- 不将无法解析的聊天引用标记直接显示给读者。

### 部署与个人状态

- 本地构建成功，项目子路径下的资源和深层链接正常。
- 草稿与不应公开的内容不进入发布文件及搜索索引。
- 页面适配手机和桌面，关键操作可通过键盘完成。
- 收藏和已读在重新打开浏览器后保留，备份可导出并恢复。
- 后续自动更新重复执行不产生重复内容，失败可定位并恢复。

测试重点放在日期计算、筛选组合、导入校验与路由等容易出现实际错误的逻辑；视觉与阅读体验通过浏览器检查验证。

## 13. 需要在实施前确定的事项

1. 内容是否允许公开访问，还是必须仅本人可见。
2. 是否接受 Briefing Atlas / 科技简报图志作为最终名称。
3. 历史内容从哪一天开始迁移，以及可获取的内容范围。
4. 首版是否需要收藏；若需要，是否接受浏览器本地保存。
5. 网站采用独立项目地址还是自定义域名。

除可见性会影响基础架构外，其他未确定项可以先按本方案默认值制作可预览版本，再根据实际体验调整。
