# 验证记录

## Live2D 看板娘（2026-09-09）

- 全站使用已有 `yibei_3` 模型，桌面右下角展示、手机默认收起，实际渲染边界参与缩放，阅读页避让悬浮阅读工具。工具栏提供可扩展动作接口。
- 核对六个指定部件及后代网格透明度持续为 `0`，包括待机和点击动作之后；真实 Core 参数采样确认眨眼、呼吸和鼠标跟随变化。人物点击、键盘台词、单次动作恢复、页面控件点击穿透及收起后的资源释放均通过。
- 76 项 Node 测试、类型检查、lint 通过；依赖审计为 0 个已知漏洞。
- 根路径完整浏览器回归 39 项通过；另一个手机专用案例在桌面按设计跳过。覆盖看板娘、搜索、阅读排版及图库；看板娘额外检查手机首次访问不请求模型、加载失败仍可收起、选择记忆、阅读设置避让、打印隐藏及 320px 窄屏无横向溢出。
- 根路径与 `/briefing-atlas` 子路径生产构建和产物验证均通过，验证 76 个页面、60 个新闻锚点、资源路径与公开产物隐私。子路径下看板娘的 5 项有效浏览器检查通过；最终预览产物恢复为根路径版本。
- 已查看桌面、手机展开状态及窄屏阅读页截图。Core 仍从 Live2D 官方地址按需加载；浏览器验证需要可访问该地址。

## 内容与日期清单（2026-09-09）

- 当前收录 12 期、60 条，连续覆盖 2026-08-28 至 2026-09-08，每期 5 条；日期清单与同步状态一致。
- 2026-09-05 的五条新闻均有完整正文、摘要、标签和可点击来源，来源发布日期不晚于简报日期；背景材料与当期进展分别说明。
- 该期包含 5 组图库、8 张配图：Armature 研究封面、3 张 NeoMME 技术图、Melody Flip 产品界面、Naver 会谈照片及 2 张 Google Research 实验图。每张均有中文替代文本、说明、署名和来源链接；图片保持源站远程引用，未声明取得自托管许可。
- 71 项 Node 测试、类型检查与 lint 通过；生产构建与产物验证通过，覆盖 76 个页面、60 个新闻锚点、搜索正文一致性及公开产物隐私检查。
- Chromium 1280 px 和 390 px 视口检查通过：五个独立阅读页、日期搜索、9 月 4—6 日相邻导航、公式渲染及无横向溢出；查看桌面简报页和手机阅读页截图。未触发远程发布。
- 两种视口均逐张验证 8 张远程图片实际加载成功，图库切换、放大预览、原图入口和来源图注正常；整期全文显示 5 组图库，日期搜索仍返回 5 条且不含图库标记与图片网址。正文、摘要、新闻编号和内容修订号保持一致，图片计入格式修订。

## 复杂搜索升级（2026-09-09）

- 搜索核心支持括号、`AND` / `OR` / `NOT`、一元 `-` / `+`、连续短语、字段限定、日期限定、中文别名和安全的英文前缀匹配；无效表达式、日期、未知字段、超长输入、过深嵌套和异常权重均停止出结果并返回诊断。
- 相关度使用字段权重、逆文档频率、词频饱和和正文长度归一化；标题、摘要、正文、主题、机构、来源和导读均可单独调整。结果提供命中字段、稳定排序、日期排序、分页和正文摘录。
- 搜索状态（查询、范围、匹配方式、日期、已读、排序和自定义权重）可编码到 URL，输入法合成期间不提前提交，浏览器返回 / 前进、刷新和复制链接均已验证。
- 新增 22 项 Node 搜索测试，全部 69 项 Node 测试通过；类型检查与 lint 通过。覆盖语法优先级、错误与资源限额、英文词边界、中文 / 全角 / Unicode 高亮、长短语裁剪、权重反转及零权重、重复关键词去重、日期边界、图库元数据排除、URL 往返和 WebMCP 只读分页。
- Chromium 桌面与手机视口共 18 项浏览器测试通过，覆盖真实索引与阅览页跳转、权重排序、链接复制、刷新与历史导航、中文输入法合成事件、分页重置、收藏与已读、主题 / 机构 / 日期组合、任一关键词、异常索引重试，以及深浅主题下的窄屏布局。另查看了 1280 px 桌面和 390 px 手机截图；320、390、640 px 视口均无横向溢出。输入法测试模拟浏览器合成事件，未操作宿主输入法面板。
- 根路径与 `/briefing-atlas` 子路径均完成生产构建、70 个页面 / 55 条新闻的产物验证及浏览器回归；最终产物恢复为根路径版本。发布 workflow 的 LTS 作业已加入 Chromium 搜索回归门禁，本轮未运行远程 workflow。
- 单次性能抽查：55 条真实内容的索引构建约 9 ms，5 个代表查询约 0.2–4 ms，首屏摘录约 0.6–6 ms；复制公开内容组成的 5,000 条模拟集合索引构建约 224 ms，5 个查询最高约 28 ms。此为本地逻辑耗时抽查，不含下载、页面绘制，也不是性能承诺。
- WebMCP 适配器的参数校验、共用排序与分页已有单元测试；真实实验性浏览器注册与生命周期仍未验证。搜索为字面检索，不做语义扩展、拼写纠错或分词同义词扩展。

## 图片与引用恢复（2026-09-08）

- 从已登录原对话页面读取 11 期、55 条新闻，展开 53 个原图库，重新导入 239 个图片条目和 113 处引用链接。图片共有 235 个不同地址，逐一 GET 检查均返回 HTTP 200 和图片 Content-Type；检查在收到响应头后取消正文流，不保存图片文件。
- 原始导出与资源批次的消息文字逐条相等；同步前后原稿摘要值、55 个新闻编号、新闻摘要及内容修订号不变，每期格式修订递增一次。重复资源导入及随后文本导入均无改写，资源保留。
- 44 项自动测试、类型检查和 lint 通过。新增覆盖安全 URL、图片渲染、元数据保留、仅资源更新、上下文错配拒绝、引用编号复用，以及阅读时长和搜索排除图片元数据。
- 根路径与 `/briefing-atlas` 子路径构建、产物验证均通过：15 个页面、55 个新闻锚点、资源前缀、正文一致性和公开产物隐私检查。最终产物恢复为根路径版本。
- 浏览器检查 1280 px 桌面及 390 px 窄屏：图库显示原图，无页面横向溢出；图库键盘横向滚动可用；抽查已加载 12 张原图，无失败状态。图片替代文本正确显示 Markdown 转义字符。搜索摘录没有图库标记或图片长网址，最新一期阅读时长按正文计为约 11 分钟。
- 39 处组合引用的附加网址仍无法明确对应；8 月 28 日和 8 月 30 日的第 5 条新闻在原文中有图片查询描述，但原页面未显示图片，保留缺失提示。原图远程引用未核验转载许可。故障图片的界面降级有渲染测试，尚未单独做真实失效图片的浏览器故障注入。

本次完成本地导入和构建。未触发远程发布；此前其他功能的完整交互回归边界仍见下文。

## Workflow 兼容性与部署清理（2026-09-08）

- 使用官方发行包并验证 SHA-256，在 Windows 上分别用 Node.js 22.23.2、24.20.0（LTS）和 26.8.1（Current）完成 `npm ci`、`npm test`、`npm run typecheck`、`npm run lint`、`npm run build` 和 `npm run verify:build`，全部通过。构建与产物验证使用 `/briefing-atlas` 子路径。
- 每个版本通过 38 项测试，其中 15 项新增部署清理测试覆盖：保留最新成功记录、同一提交重复发布、其他环境隔离、超过 100 条记录、仅一条记录、空列表、提交不匹配、非成功状态及 API 失败时停止。
- `actionlint` 检查发布 workflow 通过。所有 JavaScript Actions 使用官方已发布的 Node.js 24 运行时版本；构建矩阵动态解析 Node.js 22、最新 LTS 与最新 Current。
- 清理作业依赖部署成功，只有该作业拥有 `deployments: write` 权限。先核对最新记录，再将旧记录标记为 `inactive` 并删除；API 测试使用模拟客户端。
- 本轮没有触发远程 GitHub Actions，也没有实际删除线上部署记录。Ubuntu runner 上的执行、仓库令牌权限和真实清理结果需在推送后手动运行发布 workflow 验证。

## 本轮已执行

- 已读取的 11 期、55 条原稿与保存内容逐期完整对照，忽略 Markdown 包装与空白后正文、顺序、导语、结语、链接、代码和公式一致。
- Node 逻辑测试覆盖内容与日期校验、代码内标题、公式、空标签、引用与缺图、重复输入不写入、同日冲突、修订审阅、稳定编号重排、格式更新、跨期更正、迟到日期补齐、截断输入、并发锁、中断恢复、外部编辑冲突、状态丢失，以及构建 / 部署 / 线上验证失败的分阶段重试。
- TypeScript 类型检查与 lint。
- 真实内容生产构建；15 个阅读页面、55 个新闻锚点、搜索正文与同版清单一致、示例拒绝、草稿排除、资源引用与公开产物隐私检查。
- 依赖清理后 npm 安装成功，审计未报告已知漏洞。

最新根路径、子路径和线上核对结果见 [实施记录](../doc/ARCHIVE_IMPLEMENTATION.md)。测试中的迟到原稿、故障注入与状态恢复是可重复的逻辑验收，不能替代三次真实定时运行或电脑关闭时的云端验收。

## 阅读体验待验证边界

本轮保留已有交互，并新增手机目录、阅读进度、导语和结语及块级数学公式。没有执行完整浏览器交互回归，以下仍为待检查项：

- 手机、桌面及 200% 缩放下的排版；深浅主题下公式与来源链接。
- 键盘日历导航、日期直达、未读筛选、复制深链与自动展开正文。
- 收藏、已读、字号和阅读模式跨刷新保留，备份合并、非法备份与禁用存储降级。
- WebMCP 在支持环境中的注册、执行与清理；普通搜索在不支持时继续工作。

产品界面不解释内部归档工作流，不展示来源对话标识、内部摘要值、同步状态或私人文件路径。
